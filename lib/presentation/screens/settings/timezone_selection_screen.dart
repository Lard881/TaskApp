import 'package:flutter/material.dart';

class TimeZoneSelectionScreen extends StatelessWidget {
  const TimeZoneSelectionScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Time Zone')),
      body: const Center(child: Text('Coming in Wave 6')),
    );
  }
}
